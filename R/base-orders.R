dk_orders <- list(
  HOCKEY = list(
    CLASSIC = c('C', 'C', 'W', 'W', 'W', 'D', 'D', 'G', 'UTIL'),
    SHOWDOWN = c(),
    TIERS = c()
  )
)


fd_orders <- list(
  HOCKEY = list(
    CLASSIC = c('C', 'C', 'W', 'W', 'W', 'W', 'D', 'D', 'G'),
    SHOWDOWN = c(),
    TIERS = c()
  )
)


yahoo_orders <- list(
  HOCKEY = list(
    CLASSIC = c('C', 'C', 'W', 'W', 'W', 'D', 'D', 'G', 'G'),
    SHOWDOWN = c(),
    TIERS = c()
  )
)

base_orders <- list(
  DRAFTKINGS = dk_orders,
  FANDUEL = fd_orders,
  YAHOO = yahoo_orders
)
